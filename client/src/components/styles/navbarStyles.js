const navbarStyles = theme => ({
    color: {
        background: "transparent",
        boxShadow: "none",
    },
    navBarText: {
        color: "white"
    },
    Button: {
        color: "white"
    }
})

export default navbarStyles